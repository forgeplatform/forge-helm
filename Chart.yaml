apiVersion: v2
name: forail
description: Forail Platform — automation, RBAC, EDA, observability (k8s deployment)
type: application
version: 1.0.0
appVersion: "2026.05.0"
# forail-operator lives in its own repo (forail-platform/forail-operator) and
# is installed separately via its own helm chart:
#   helm install forail-operator <operator-chart-source> -n forail --set forail.token=<PAT>
# When the operator chart is published to an OCI registry / chart museum,
# we'll add it back here as a `dependencies:` entry.
home: https://forail-platform.github.io
sources:
  - https://github.com/forail-platform/forail-devops
  - https://github.com/forail-platform/forail-backend
  - https://github.com/forail-platform/forail-frontend
maintainers:
  - name: Krstan Vjestica
    email: office@krletron.xyz
