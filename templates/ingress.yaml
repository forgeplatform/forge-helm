{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: forail
  namespace: {{ include "forail.namespace" . }}
  labels:
    {{- include "forail.componentLabels" (dict "root" . "component" "ingress") | nindent 4 }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts: [{{ .Values.ingress.host | quote }}]
      secretName: {{ .Values.ingress.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: forail-web
                port:
                  number: 8013
          - path: /admin
            pathType: Prefix
            backend:
              service:
                name: forail-web
                port:
                  number: 8013
          # SPA bundle (Vite output, base path /static/forail/) — must
          # match BEFORE /static so Django staticfiles fallback applies
          # only to other prefixes (admin, rest_framework, etc.).
          - path: /static/forail
            pathType: Prefix
            backend:
              service:
                name: forail-frontend
                port:
                  number: 80
          - path: /static
            pathType: Prefix
            backend:
              service:
                name: forail-web
                port:
                  number: 8013
          - path: /sso
            pathType: Prefix
            backend:
              service:
                name: forail-web
                port:
                  number: 8013
          - path: /websocket
            pathType: Prefix
            backend:
              service:
                name: forail-web
                port:
                  number: 8015
          - path: /
            pathType: Prefix
            backend:
              service:
                name: forail-frontend
                port:
                  number: 80
{{- end }}
